spring:
  application:
    name: service-order
  cloud:
    nacos:
      config:
        server-addr: 193.112.22.138:8848 # nacos的服务端地址
        namespace: public
        group: SEATA_GROUP
    alibaba:
      seata:
        tx-service-group: order-service